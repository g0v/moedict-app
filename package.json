{
	"name": "moedict-app",
	"private": true,
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"postinstall": "git submodule update --init --depth 1",
		"dev": "vite",
		"build": "tsc -b && vite build",
		"build:android": "npm run prepare-data && npm run build && npx cap sync android",
		"verify:android": "sh scripts/verify-apk.sh",
		"smoke:android": "sh scripts/smoke-android.sh",
		"build:ios": "npm run prepare-data && npm run build && npx cap sync ios",
		"build:macos": "npm run build && sh scripts/build-macos.sh",
		"prepare-data": "sh scripts/prepare-data.sh",
		"lint": "eslint ."
	},
	"dependencies": {
		"@capacitor/ios": "^7.6.0",
		"@capacitor/status-bar": "^7.0.6",
		"fuse.js": "^7.1.0",
		"react": "^19.1.1",
		"react-dom": "^19.1.1",
		"react-router-dom": "^7.13.1"
	},
	"devDependencies": {
		"@capacitor/android": "^7.2.0",
		"@capacitor/cli": "^7.2.0",
		"@capacitor/core": "^7.2.0",
		"@types/react": "^19.1.10",
		"@types/react-dom": "^19.1.7",
		"@vitejs/plugin-react": "^5.0.0",
		"typescript": "~5.8.3",
		"vite": "^7.3.2"
	}
}
